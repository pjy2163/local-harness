interface:
  display_name: "Harness Maintenance"
  short_description: "프로젝트 하네스만 최소 수정하고 관련 행동 eval로 확인합니다"
  default_prompt: "Use $project-harness to inspect or update only the project harness. Preserve unrelated work and safety boundaries, keep Luna max, and verify the changed behavior with only relevant static checks and lightweight evals."
